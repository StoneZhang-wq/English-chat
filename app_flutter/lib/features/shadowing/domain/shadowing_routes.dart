/// Shadowing 分区嵌套 [Navigator] 的路由名（与 Kotlin `AppRoute.ShadowingSession` 语义一致）
class ShadowingRoutes {
  ShadowingRoutes._();

  static const home = '/';
  static const session = '/shadowing/session';
}
