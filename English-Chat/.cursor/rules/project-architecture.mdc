---
description: English-Chat 项目架构、目录职责与开发者约束，修改代码时请遵守
alwaysApply: true
---

# English-Chat 项目架构规则

## 技术栈与入口

- **后端**：FastAPI，异步（async/await）
- **数据库**：MySQL，ORM 为 SQLAlchemy（async，aiomysql）
- **缓存**：Redis（AsyncRedisCache）
- **前端**：Jinja2 模板 + 静态资源（HTML/CSS/JS），无 SPA 框架
- **认证**：Bearer Token；前端仅用 **sessionStorage** 存 `token`、`current_account`、`current_user_id`（不用 localStorage，关页即失效）
- **入口**：`main.py`，lifespan 中清 Redis、建表、从 JSON 初始化场景数据

## 文件组织结构（完整目录树）

```
English-Chat/
├── main.py                 # FastAPI 应用入口，路由挂载、静态/模板路径、lifespan
├── config.py                # 配置类 Config，从 .env 读取（数据库、Redis、LLM、TTS 等）
├── run.py                   # 可选运行入口
├── routers/                 # HTTP 路由层，仅处理请求/响应与依赖注入，业务逻辑放在 service/crud
│   ├── api.py               # /api：练习、学习推荐、对话生成、practice/start|respond|end、复习生成、保存进度等
│   ├── auth.py              # /api/account：注册、登录、登出、当前用户
│   ├── admin.py             # /api/admin：用户管理（列表、分页、统计、增删改），仅 username=admin 可访问
│   ├── ai_chat.py           # /api/chat：自然对话 /practice、学习摘要 /summary
│   ├── scene.py             # /api/scene-npc：大/小场景、NPC、dialogue/learn|immersive、immersive-chat、report
│   ├── audio.py             # /api/audio：TTS 文件服务 /tts、/english_dialogue/{dialogue_id}、/temp/{token}
│   └── websocket.py         # WebSocket 连接管理
├── service/                 # 业务与外部服务
│   ├── agent.py             # ChatAgent：LLM 调用、TTS 流程、ASR、会话流程 generate_chat_flow
│   ├── database.py          # 异步引擎、AsyncSessionLocal、get_db 依赖
│   ├── redis_cache.py       # 异步 Redis 缓存封装
│   ├── conn_manager.py      # WebSocket 连接管理（按 user_id/channel）
│   ├── scene_manager.py     # 场景/对话数据统一入口：get_dialogues、get_big_scenes、get_small_scenes 等
│   ├── init_scene_data.py   # 启动时从 resource 下 JSON 增量同步到 BigScene、SmallScene、Dialogue
│   ├── practice.py          # 练习模式逻辑：首轮/下一轮、hints、完成判定等
│   ├── prompt.py            # 各类 LLM 提示词
│   ├── doubao.py            # 豆包 TTS/ASR
│   ├── openai.py / gemini.py # LLM 调用
│   └── protocols/          # 豆包 TTS 等协议
├── crud/                    # 数据库增删改查，不包含业务规则
│   ├── users.py, chat.py, dialogue.py, scene.py, scene_study.py, diary.py
├── models/                  # SQLAlchemy ORM，与表一一对应
│   ├── base.py, users.py, chat.py, scene.py, dialogue.py, scene_npc.py, diary.py
├── utils/                   # 通用工具与中间件
│   ├── auth.py              # get_current_user、get_current_admin（admin 仅 username=="admin"）
│   ├── response.py          # 统一响应 success_response 等
│   ├── logger_manager.py, exception_handlers.py, security.py, tools.py
├── frontend/
│   ├── templates/           # Jinja2：voice_chat.html, user_management.html, scene.html 等
│   └── static/css、static/js  # core.js 负责 API 封装，token 从 sessionStorage 读取
├── resource/                # 静态配置数据，启动时同步到 DB
│   ├── scenes/              # scene_npc_index.json、scenes.json
│   └── dialogues/           # dialogues.json
├── data/                    # 运行时生成，不提交版本库
│   ├── audio/tts/           # 用户级 TTS，按 user_id 分子目录
│   └── english_dialogue/    # 固定剧本 TTS，按 dialogue_id 分目录
├── characters/              # 角色配置（名称、提示等）
└── doc/                     # PROJECT_ARCHITECTURE.md、English-Chat-AI-接口手册.md
```

## 模块约定

- **routers**：解析请求、校验参数、调用 service/crud、返回统一响应；鉴权用 `Depends(get_current_user)` / `Depends(get_current_admin)`，会话用 `Depends(get_db)`，Agent 用 `Depends(get_agent_service)`。场景/对话数据**必须**通过 `scene_manager` 获取，**禁止**在 router 或其它业务层直接 crud Dialogue/BigScene/SmallScene。
- **service**：scene_manager 为场景/对话的**唯一数据入口**；doubao TTS 路径：用户/沉浸式 → `./data/audio/tts/{user_id}/`，剧本 → `./data/english_dialogue/{dialogue_id}/`；init_scene_data 仅按主键增量插入，不删除已有记录。
- **crud**：仅做增删改查与简单 DTO 转换；业务规则放在 service。
- **models**：新增表需在 `models/__init__.py` 导出，并在 `main.py` lifespan 前 import，以便 `Base.metadata.create_all` 建表。
- **前端**：token、current_account、current_user_id 仅用 sessionStorage；登出或 pagehide 时调用登出接口并清空 sessionStorage；请求头 `Authorization: Bearer <token>`。

## 数据库表用途

| 表名 | 用途 |
|------|------|
| **users** | 用户账号与档案（用户名、邮箱、密码、英语水平、年龄、职业、目标等）；管理员仅 username=="admin" 判断 |
| **chat_sessions** | 聊天会话，归属 user_id，含 scene_type、title、创建/更新时间 |
| **chat_messages** | 会话消息，归属 session_id，含 role、character、content；自然对话与沉浸式内容存于此 |
| **big_scene** | 大场景（如 daily、food、transport），id、name、sort_order |
| **small_scene** | 小场景，归属 big_scene_id，含 immersive_scene_id |
| **dialogue** | 对话配置：dialogue_id、big/small_scene_id、npc、usage(learn/review/immersive)、content(JSON)、core_sentences、npc_name、user_goal 等 |
| **npc_learn_progress** | NPC 学习进度，每用户一条，process 为已学 dialogue_id 列表的 JSON 数组 |
| **diary** | 学习日记，可选关联 scene_type、session_id |

## 开发者必须遵守的约束

1. **场景与对话数据**：仅通过 `service.scene_manager` 的 `get_dialogues`、`get_big_scenes`、`get_small_scenes`（及衍生接口）获取；不得在 routers 或其它 service 中直接对 Dialogue、BigScene、SmallScene 表执行 crud 查询。
2. **认证与前端状态**：前端仅使用 sessionStorage 存储 token 与当前用户信息；管理员判断仅依据 `username == "admin"`。
3. **TTS 存储**：用户自然对话与沉浸式单条回复的 TTS 存于 `./data/audio/tts/{user_id}/`；固定剧本（练习/复习）的 TTS 存于 `./data/english_dialogue/{dialogue_id}/`，命名与现有练习模式一致以便复用。
4. **会话与历史**：沉浸式对话在 GET dialogue/immersive 时创建 ChatSession 并返回 session_id；immersive-chat 将历史从该 session 的 chat_messages 中读取并作为 LLM 上下文，不得仅依赖前端上传的 history 作为唯一上下文。
5. **数据初始化**：场景与对话的初始数据来自 `resource/` 下 JSON；init_scene_data 仅做按主键增量插入，不删除已有记录。
6. **新增表或模型**：在 `models/` 中定义并在 `models/__init__.py` 与 `main.py` 的模型 import 中登记，以保证 lifespan 建表正确。

修改或扩展本系统时请勿绕过以上约定。详细说明见 `doc/PROJECT_ARCHITECTURE.md`。
