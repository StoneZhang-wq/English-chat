---
description: 临时测试代码的清理约定与可复用逻辑（公式）的集中存放位置
alwaysApply: true
---

# 代码卫生与复用（公式）

## 临时测试 / 一次性验证

- **目标**：测试结束后，主业务目录里不残留「只用过一次」的脚本或调试代码。
- **做法**：
  - 临时脚本、随手验证 → 放在工作区根目录 **`scratch/`** 下（按需要建子目录）；**勿把密钥写进文件**。
  - 需要可回归的测试 → 放在正式测试目录：**Flutter** 的 `app_flutter/test/`；**backend** 的 `tests/`（或模块约定目录），**长期保留**。
  - 在功能分支里做的实验，合并进主分支前应删除或移出交付路径。
- **Agent**：为用户做「只验证一次」的临时代码时，优先写在 **`scratch/`**；任务完成且用户未要求保留时，**删除本次新增的临时文件**（正式单元测试除外）。

## 可复用逻辑（同一「公式」只写一处）

- **原则**：相同算法、校验、格式化、提示词拼装等，**只实现一次**，其它地方 **import / 调用**。
- **存放（本仓库约定）**：
  - **`app_flutter/`**：可复用 Dart 逻辑优先放 **`lib/core/`**；与后端对接放 **`lib/data/`**；禁止在多个 Widget/Screen 里复制同一段长逻辑。
  - **`backend/`**：业务可复用 → **`services/`**；纯工具函数 → **`utils/`**；路由 **`routers/`** 只做参数校验与编排，不写大块可复用算法。
- **命名**：按主题拆文件（例如 `PromptBuilder`、`text_normalize`），避免单文件无限堆叠。

## 说明

- 规则**约束协作与习惯**；无法自动删除磁盘文件，靠上述约定与 Agent 在收尾时清理 **`scratch/`** 中的临时增量。
