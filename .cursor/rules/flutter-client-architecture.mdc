---
description: MyEnglishChat Flutter 客户端主线（Android+iOS）工程约定与迁移规则
alwaysApply: true
---

# MyEnglishChat Flutter 客户端协作规则（主线）

## 0. 定位

- **主产品客户端**：`app_flutter/`（Flutter，Android + iOS）。
- **后端**：`backend/`（Python + FastAPI）。
- **`MyEnglishChatApplication/`**：历史 Kotlin/Compose 客户端，仅作迁移对照与临时验证；新功能默认不再在此目录继续堆叠（除非用户明确要求）。
- **`English-Chat/`**：历史网页，仅参考；禁止扩展其 HTML/JS/CSS 作为线上产品能力。

## 1. Flutter 工程结构（约定）

目标是把「录音/转写/对话」做成可测试、可替换实现的模块，而不是散落在 UI 文件里。

推荐目录：

```
app_flutter/
├── lib/
│   ├── app/                 # App 根：主题、路由、依赖注入入口
│   ├── core/                # 可复用：日志、错误、常量、网络封装、utils
│   ├── data/                # API client、DTO、Repository（对接 backend）
│   ├── features/
│   │   ├── practice/        # 练习模式（文本/语音）
│   │   ├── shadowing/       # 跟读/场景
│   │   └── p2p/             # 真人（后续落地）
│   └── main.dart
├── test/
└── pubspec.yaml
```

依赖方向（强约束）：

- `features/*` → `data/*` / `core/*`
- `data/*` → `core/*`
- **禁止** `data/*` 依赖 `features/*`

## 2. 语音链路（端侧离线 Vosk）

- **端侧优先**：录音 → 本地转写（Vosk）→ 再把转写文本送给 `backend` 获取 LLM 回复。
- UX 规则（借鉴 English-Chat）：
  - 松手后立刻上屏语音气泡（先显示「转写中…」），转写完成后更新同一条消息。
  - 停录的“边界”必须与手势结束对齐：**先 stop-capture（轻量）再异步 finalize（IO）**，避免多录静音。
- 模型下载：
  - 首次下载提示清晰、非阻塞；进入练习/场景时后台预取；避免首次发语音才下载。

## 3. Base URL 与环境

- 客户端的 Base URL 必须可配置（debug / release、真机 / 模拟器）。
- 客户端不得内置任何 LLM/云厂商密钥；密钥仅在 `backend/`。

## 4. 文档更新要求

- 当 Flutter 端目录结构、API、语音链路、RTC 选型发生变化时，需同步更新：
  - `docs/TECH-ARCHITECTURE.md`

